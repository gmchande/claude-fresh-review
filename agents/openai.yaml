interface:
  display_name: "Claude Review"
  short_description: "Visible Claude gate for one or more repos"
  default_prompt: "Use $claude-review to review this change across every affected repo, then verify and checkpoint before editing."

policy:
  allow_implicit_invocation: false
