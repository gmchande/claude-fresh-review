interface:
  display_name: "Claude Review"
  short_description: "Single-session interactive Kimi review gate"
  default_prompt: "Use $claude-review to run one interactive Kimi review, independently assess its findings, and checkpoint before editing."

policy:
  allow_implicit_invocation: false
